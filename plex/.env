# Plex Configuration
#
# To claim your Plex server during initial setup, get a claim token from:
# https://www.plex.tv/claim/
# Then uncomment and add it to the PLEX_CLAIM variable in docker-compose.yml
#
# Media directory mapping:
# Update the /data/media path in docker-compose.yml to point to your actual media location
